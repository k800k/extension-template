<!-- SPDX-License-Identifier: GPL-3.0-or-later -->

# Example Source privacy assessment

The fixture example performs no network requests, stores no state, requests no authentication, and transmits no user or device data. Its example URLs are reserved `example.com` values and are never requested by tests.
